# PLAN — 習慣トラッカー

## 1. 概要

毎日続けたい習慣（運動・読書・水分補給など）を登録し、その日に実行したかどうかをチェックボックス1タップで記録できる静的単一ページアプリを作る。習慣一覧と日付ごとのチェック記録は localStorage に永続化され、リロードしても復元される。チェックは「今日の日付」に紐づけて保存するため、日付が変わると当日のチェックは未実行状態に戻り、毎日の記録として機能する。過去の記録は各習慣の直近7日間の履歴ドットと連続実行日数として表示され、継続状況をひと目で確認できる。サーバコード・外部 API・ビルドツールは使わず、`public/index.html` 単一ファイル（CSS/JS インライン）で完結させる。

## 2. 意図（明示）

運動や読書、水分補給など毎日続けたい習慣がある人が、その日やったかどうかを1タップで記録し、翌日以降もアプリを開くたびに継続状況をひと目で確認するために使う。

## 3. 受け入れ条件

- [ ] テキスト入力欄に習慣名を入力して追加操作（追加ボタン押下または Enter キー）を行うと、その習慣が一覧に表示される
- [ ] 一覧の各習慣にはチェックボックスがあり、チェックを入れると「今日実行済み」として記録され、チェック状態が UI に反映される
- [ ] ページをリロードしても、登録済みの習慣一覧と当日のチェック状態がそのまま復元される
- [ ] 習慣の削除ボタンを押すと、該当習慣が一覧から消え、localStorage 上の習慣データとチェック記録からも削除される（リロード後も復活しない）
- [ ] 空文字・空白のみの入力では習慣が追加されない
- [ ] 習慣が1件もないときは、空状態メッセージ（例:「習慣を追加して始めましょう」）が表示される
- [ ] 日付が変わって（翌日に）アプリを開くと、当日のチェックボックスは未チェック状態で表示される。前日のチェック記録は localStorage に保持されたまま消えない（Playwright の Clock API で日付を進めて検証する）
- [ ] 各習慣の行に、直近7日間の実行履歴（実行済み/未実行を示す7個のドット）と連続実行日数（今日または昨日から遡って連続でチェックされた日数）が表示され、継続状況をひと目で確認できる

## 4. 実装方針

### 技術

- バニラ JS・CSS/JS インラインの `public/index.html` 単一ファイル。フレームワーク・外部ライブラリ・ビルドなし（AGENTS.md 不変スタック）
- 配信は既存の `wrangler.jsonc`（Cloudflare Workers assets）のまま変更しない
- favicon は `<link rel="icon" href="data:image/svg+xml,...">` のインライン data URI（チェックマーク＋カレンダー風のテーマに合った絵柄）

### データモデル（localStorage）

- キー: `habit-tracker:v1` に単一 JSON オブジェクトを保存
  ```json
  {
    "habits": [{ "id": "h_1712345678901", "name": "運動" }],
    "checks": { "2026-08-08": ["h_1712345678901"] }
  }
  ```
- `habits`: 登録順の配列。`id` は `Date.now()` ベースの一意文字列
- `checks`: ローカル日付文字列（`YYYY-MM-DD`）→ その日に実行済みの habit id 配列。日付をキーにするため翌日は自動的に未チェック表示になり、過去の記録も保持される
- 習慣削除時は `habits` から除去し、`checks` の全日付からも該当 id を除去する
- 読み込み時に JSON.parse を try/catch し、壊れたデータは初期状態にフォールバック

### レイアウト

- センタリングされた単一カラムのメインコンテナ（`max-width: 480px` 程度）。上から「タイトル → 追加フォーム（テキスト入力＋追加ボタン） → 習慣一覧 → 空状態メッセージ」
- 習慣一覧の各行は「チェックボックス・習慣名・直近7日の履歴ドット・連続日数バッジ（例: 「3日連続」）・削除ボタン」で構成する。履歴ドットは古い日→今日の順に7個並べ、実行済みは塗りつぶし・未実行は輪郭のみで表現する
- body はセンタリングに flex を使う場合 `flex-direction: column` にする（AGENTS.md の footer 制約対応）
- hub フッターは AGENTS.md 指定のマークアップをメインフローの最下部に配置する

### 主要関数

- `todayKey()`: ローカルタイムゾーンで `YYYY-MM-DD` を返す
- `loadState()` / `saveState(state)`: localStorage との読み書き（parse 失敗時は初期状態）
- `addHabit(name)`: trim して空なら何もしない。habit を追加して保存・再描画
- `toggleCheck(id, checked)`: `checks[todayKey()]` に id を追加/除去して保存・再描画
- `deleteHabit(id)`: `habits` と `checks` 全日付から id を除去して保存・再描画
- `recentDays(n)`: 今日を含む直近 n 日分の日付キー配列を返す（履歴ドット用、n=7）
- `streak(id)`: 今日チェック済みなら今日から、未チェックなら昨日から遡り、連続でチェックされた日数を返す
- `render()`: state から一覧 DOM を全再構築（習慣0件なら空状態メッセージ表示）。各行に履歴ドット7個と連続日数バッジを描画する。habit 名は `textContent` で挿入し XSS を防ぐ

### テスト

- `tests/app.spec.ts`: 雛形スモークテストを残しつつ、上記受け入れ条件それぞれに対応するテストを追加（追加表示 / チェック記録 / リロード復元 / 削除の永続反映 / 空入力拒否 / 空状態表示 / 日付変更時のリセット / 履歴ドットと連続日数の表示）
- 日付変更テスト: Playwright の Clock API（`page.clock.install({ time })` → チェック → `page.clock.setFixedTime(翌日)` → リロード）で `Date` をモックし、「翌日はチェックボックスが未チェックに戻り、前日分の記録が localStorage の `checks` に残っている」ことを検証する
- 継続状況テスト: `page.addInitScript` で過去日分の `checks` を localStorage に注入した状態でページを開き、履歴ドットの塗り分けと連続日数バッジの値が注入データと一致することを検証する
- `scripts/persist-test.mjs`: scenario を「習慣追加＋チェック」、verify を「リロード後に一覧とチェック状態が復元されている」に書き換える
